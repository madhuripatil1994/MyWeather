const initialState = {
    users : [],
    loading: false
}

const uviReducer = (state = initialState, action) => {
    switch (action.type) {
      
      default:
        return state
    }
}

export default uviReducer;